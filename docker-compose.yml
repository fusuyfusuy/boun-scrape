services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - schedules-data:/data
    environment:
      - DB_PATH=${DB_PATH:-/data/schedules.db}
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    restart: always

volumes:
  schedules-data:
